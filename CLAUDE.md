# Southern 100 — Notes

## Reference material

- **XISF 1.0 spec** (PixInsight's native image format): https://pixinsight.com/doc/docs/XISF-1.0-spec/XISF-1.0-spec.html — flagged by Dan as a new revision worth keeping on hand. Relevant if we ever need to read/write XISF files or their embedded metadata (e.g. for pulling exposure/ADU/gain data directly out of a real sub rather than typing it in by hand).
